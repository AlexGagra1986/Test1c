Если (РежимЗаписи = РежимЗаписиДокумента.ОтменаПроведения 
            ИЛИ РежимЗаписи = РежимЗаписиДокумента.Проведение) И ПеремещениеТоваров ТОгда
	     Отказ = Истина;
         Сообщить("Проведение и отмена проведения для вашей роли недоступно", СтатусСообщения.Важное); 
    КонецЕсли;
